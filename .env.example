# Django Settings
DJANGO_ENVIRONMENT=development
DJANGO_SECRET_KEY=django-insecure-change-this-in-production-xyz123abc456
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
DATABASE_NAME=farmvision
DATABASE_USER=farmvision_user
DATABASE_PASSWORD=changeme_database_password
DATABASE_HOST=localhost
DATABASE_PORT=5432

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# Celery Configuration
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# File Paths
MEDIA_ROOT=/path/to/media
STATIC_ROOT=/path/to/static

# Security Settings (Production)
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False

# Email Configuration (Optional)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-email-password
DEFAULT_FROM_EMAIL=noreply@farmvision.com

# CORS Settings (if needed)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Logging
LOG_LEVEL=INFO

# YOLOv8 Model Settings
DEFAULT_YOLO_MODEL=yolov8n.pt
YOLO_CONFIDENCE_THRESHOLD=0.25
YOLO_IOU_THRESHOLD=0.45

# WebODM Integration (Optional)
WEBODM_HOST=localhost
WEBODM_PORT=8000
WEBODM_USERNAME=admin
WEBODM_PASSWORD=admin

# Performance Settings
MAX_UPLOAD_SIZE=104857600  # 100MB in bytes
ALLOWED_IMAGE_FORMATS=jpg,jpeg,png,tiff,tif

# Monitoring (Optional)
SENTRY_DSN=
